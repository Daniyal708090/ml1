# Copy to `.env` in repo root and fill in. Docker Compose reads variables from `.env` automatically.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_JSON_MODE=1
